version: '3.8'

services:
  api:
    build: .
    container_name: fastapi-login-system
    ports:
      - "1234:1234"
    environment:
      - SECRET_KEY=your-secret-key-change-this-in-production
    restart: unless-stopped
    volumes:
      - ./:/app
    command: uvicorn main:app --host 0.0.0.0 --port 1234 --reload
